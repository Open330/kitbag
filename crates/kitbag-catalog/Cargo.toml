[package]
name = "kitbag-catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Where credentials are known to live, and how to notice the rest"

[dependencies]
glob = "0.3"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
kitbag-core = { path = "../kitbag-core" }

[dev-dependencies]
tempfile = "3"
